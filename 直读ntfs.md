# Steam Deck 单硬盘双系统 + 互通游戏库详细教程

> **作者**: --River--  
> **来源**: [哔哩哔哩专栏](https://www.bilibili.com/read/cv18881740/)  
> **更新日期**: 2022年10月 (含后续更新说明)

本教程旨在指导用户在 Steam Deck 单个固态硬盘上安装 Windows 与 SteamOS 双系统，并实现两个系统间共享游戏库（NTFS格式互通）。

---

## 📋 目录

1. [准备工具](#1-准备工具)
2. [制作 PE 启动盘](#2-制作-pe-启动盘)
3. [调整硬盘分区](#3-调整硬盘分区)
4. [安装 Windows 系统](#4-安装-windows-系统)
5. [设置开机引导为 rEFInd (可选)](#5-设置开机引导为-refind-可选)
6. [Windows 安装 Steam 并添加数据盘](#6-windows-安装-steam-并添加数据盘)
7. [SteamOS 自动挂载 NTFS 分区](#7-steamos-自动挂载-ntfs-分区)
8. [常见问题与注意事项](#8-常见问题与注意事项)

---

## 1. 准备工具

此次制作双系统必须的道具如下：

*   电脑一台
*   大于等于 8GB 的 U 盘一只 (可以用 TF 卡代替，前提是可使用上述电脑访问 TF 卡)
*   键盘、鼠标
*   USB 分线器 (用来同时使用键鼠和 U 盘)
*   **文件压缩包** (需从原帖置顶评论下载，包含驱动和引导文件)
*   **Windows 10 或 11 镜像** (在官网等途径自行下载)

![准备工具示意图](https://i0.hdslb.com/bfs/article/4adb9255ada5b97061e610b682b8636764fe50ed.png@progressive.webp)

> **⚠️ 踩坑注意**：
> 最新微软官网下载的 Win11 22H2 版本安装 SteamOS 提供的音频驱动会直接蓝屏装不上，请避开此版本。**建议选择 Win10 或者 Win11 21H2 版本。**

---

## 2. 制作 PE 启动盘

有过电脑装系统经验的小伙伴可以按自己步骤来跳过此部分，只需要保证制作的 PE 启动盘内有像 **DiskGenius** 之类的硬盘管理工具即可。

没有经验的小伙伴，推荐按照如下链接，使用 **FirPE** 制作启动盘：
*   下载地址：[https://firpe.cn/page-397](https://firpe.cn/page-397)

**操作步骤**：
1.  制作好 PE 启动盘。
2.  将 **Windows 镜像文件** 和 **作者提供的全部文件** (驱动、引导文件等) 移动至启动盘内。
3.  至此，U 盘部分准备完成。后续流程中“U 盘”、“PE 盘”、“启动盘”均指此 U 盘。

---

## 3. 调整硬盘分区

1.  **进入 PE 系统**：
    *   USB 分线器连接 Steam Deck、键鼠和 U 盘。
    *   将 Steam Deck 关机。
    *   先长按 `音量-` 按钮，再短按 `电源键`。
    *   机器进入启动菜单后，松开 `音量-` 按钮。
    *   通过上下方向键选择 `USB Device` 选项，按手柄 A 或键盘回车进入 PE 系统。

    ![选择开机系统](https://www.bilibili.com/) *(注：原图链接失效，此处为操作示意)*

2.  **划分分区 (使用 DiskGenius)**：
    *   进入 DiskGenius，点击占空间最大、位于硬盘分卷排列最后的 `home` 分卷。
    *   右键 -> `调整分区大小`。
    *   给 SteamOS 和将要安装的 Win 系统都留出足够的空间。
        *   **Home 分区**：保留给 SteamOS 机身存储 (作者保留了 50GB)。
        *   **空白空间**：用于创建 Windows 分区。
    *   点击剩下的空白分区，右键 -> `创建新分区`：
        1.  **Windows C 盘**：建议 128GB，格式 **NTFS**。
        2.  **共通游戏数据盘**：剩余所有空间，格式 **NTFS**。
            *   **重要**：将此分区命名为 `Game` (也可使用其他名字，但不要用特殊字符，后面会用到)。
    *   创建分区之后要点击左上角的 **保存更改** 才算成功。

    ![划出分区之后的硬盘分卷](https://www.bilibili.com/)

> **⚠️ 踩坑注意一**：
> 1.  **Inode 报错**：正常使用一段时间的 SteamOS，缩减 home 分卷空间时有几率遇到报错“inode bitmap 中有标记为已使用的未用 inode”。
>     *   **解决方法**：只能使用官方工具重装 SteamOS，并在安装完成后的最后一个弹窗选择 **不重启**，在 SteamOS 一次都未启动过的情况下，进行分区划分工作。或者尝试恢复出厂设置 (需备份数据)。
> 2.  **空白卷被占用**：在末尾留给 Windows 的空间还处于 **未分卷状态** 时，千万不要进入 SteamOS 系统。否则会造成 SteamOS 自动将此空白卷划分入 home 卷中，导致无法被识别利用且无法重新分区。

---

## 4. 安装 Windows 系统

1.  **一键安装**：
    *   在 PE 系统桌面上打开软件 **EasyRC**。
    *   最上方选择你的 Windows 镜像。
    *   选择系统安装目标盘符：选择在第三部分中划分出来给 C 盘的 128GB 分卷。
    *   添加引导位置应该会自动选择 ESP 对应的盘符。
    *   点击确定，静等安装完成自动重启。

    ![一键装 windows](https://www.bilibili.com/)

2.  **配置 Windows**：
    *   重启后将直接进入 Windows 系统，完成常规配置。
    *   **安装驱动**：插上 PE 启动盘，找到提供的 `qudong` 文件夹，逐个安装官方驱动。
        *   **注意**：请先调整为 **横屏** 再安装显卡驱动，竖屏安装可能导致 AMD 窗口闪退。
        *   **必须安装驱动**，否则后续可能出现花屏现象。

    ![steam 官方驱动的安装方法](https://www.bilibili.com/)

3.  **关键设置**：
    *   **关闭快速启动**：在 Windows 电源管理中关闭“快速启动”。
        *   原因：否则可能导致 Win 系统无法正常关机，或在 Linux 下读取 NTFS 硬盘时被设置为只读。
    *   **关机习惯**：Windows 关机直接点关机，**不要休眠**。

---

## 5. 设置开机引导为 rEFInd (可选)

> **⚠️ 注意**：此部分教程可能已过时，作者后续推出了脚本一键替换引导工具。若遇问题请移步作者新专栏。此处仅记录原文方法。

**美化目标**：开机直接进入漂亮的选择启动系统界面，支持手柄操作。

**操作步骤**：
1.  从 PE 启动盘启动 Steam Deck。
2.  在“此电脑”中点开 `esp` 盘，进入 `efi` 目录。
3.  给 `Microsoft` 文件夹创建一个副本并命名为 `Deck` (备份)。
4.  将 `Microsoft` 文件夹内的 `Boot` 文件夹删除。
5.  将作者提供的 `Boot` 文件夹复制进 `Microsoft` 目录。
6.  重启即可进入美化后的 rEFInd 引导。
    *   **操作方式**：可通过触摸板移动鼠标，右扳机键替代鼠标左键；或使用十字方向键选择，A 键确定。

![开机引导美化成果](https://www.bilibili.com/)

> **已知问题**：
> 1.  Windows 开机启动画面可能有轻微花屏 Bug，但不影响使用。
> 2.  Windows 大更新后可能会替换开机引导，需要重新替换 `Boot` 文件夹 (建议关闭 Windows 更新或使用作者新出的脚本工具)。

---

## 6. Windows 安装 Steam 并添加数据盘

1.  **清理盘符**：
    *   右键“此电脑” -> “管理” -> “磁盘管理”。
    *   右键点击除了 C 盘和数据盘 (Game) 之外的所有分区，选择“更改驱动器号和路径”，**删除** 它们的驱动器号。
    *   确保数据盘被指派为 **D:** 盘。

    ![删除或更改驱动器号](https://www.bilibili.com/)

2.  **安装 Steam**：
    *   安装 Windows 版 Steam。
    *   进入 `Steam` -> `设置` -> `下载` -> `Steam 库文件夹`。
    *   添加 D 盘 (`D:\SteamLibrary`) 为下载盘。

    ![加载到 D:/STEAMLIBRARY](https://www.bilibili.com/)

---

## 7. SteamOS 自动挂载 NTFS 分区

以下步骤均在 **桌面模式** 下进行。

### 第一步：设置 deck 账户密码
*   **方法一**：系统设置 -> 用户 -> 设置密码。
*   **方法二**：终端输入 `passwd`，两次输入密码 (输入时不显示)。

### 第二步：修改 policy 文件 (取消 NTFS 访问密码)
1.  打开终端 (Konsole)，输入：
    ```bash
    sudo steamos-readonly disable
    ```
    (输入密码确认)
2.  打开文件管理器，进入 `rootfs` 盘符，找到以下文件并用 KWrite 打开：
    `/usr/share/polkit-1/actions/org.freedesktop.UDisks2.policy`
3.  修改第 **173** 行：
    *   原内容：`<allow_active>auth_admin_keep</allow_active>`
    *   改为：`<allow_active>yes</allow_active>`
4.  Ctrl+S 保存，输入密码确认。

    ![修改第 173 行为“yes”并保存](https://www.bilibili.com/)

### 第三步：修改 fstab 文件 (开机自动挂载)
1.  在文件管理器 `rootfs` 盘符下，进入 `/etc/` 目录，找到 `fstab` 文件并打开。
2.  在文件末尾添加以下内容 (注意 `LABEL=Game` 需与你第三步中命名的分区名称一致)：
    ```text
    LABEL=Game  /run/media/deck/Game  ntfs   defaults,nofail   0 0
    ```
3.  Ctrl+S 保存，输入密码确认。
4.  **验证**：重启后，在文件管理器查看数据盘，若没有小黄标且无需密码即可访问，说明自动挂载成功。

    ![编辑 fstab 自动挂载硬盘](https://www.bilibili.com/)

### 第四步：将数据盘添加为 Steam 游戏下载盘
1.  打开 Steam (桌面模式或游戏模式均可)。
2.  `Steam` -> `设置` -> `下载` -> `Steam 库文件夹`。
3.  点击加号，选择刚才挂载的数据盘目录 (`/run/media/deck/Game`)。
4.  系统应能直接读取到 Windows 上建立的 `SteamLibrary` 文件夹。
5.  在游戏模式下，进入 `设置` -> `存储设备`，将此分卷设为默认下载库。

    ![路径最后为 STEAMLIBRARY 文件夹](https://www.bilibili.com/)

---

## 8. 常见问题与注意事项

*   **性能问题**：经测试，NTFS 格式下的游戏帧数和功耗与原生 `/home` 分区无异，无明显性能损耗。
*   **格式选择**：
    *   **NTFS**：推荐。Windows 和 Linux 完美读写，SteamOS 支持作为游戏库。
    *   **exFAT**：不推荐。虽然双系统可读，但 **SteamOS 内的 Steam 不允许 exFAT 格式硬盘加载为游戏下载库**。
    *   **Btrfs**：如需尝试可自行研究，但配置较复杂。
*   **驱动更新**：务必避开 Win11 22H2 版本以防声卡驱动蓝屏。
*   **数据安全**：分区操作有风险，建议提前备份重要数据。

---

*本文基于 --River-- 的原创专栏整理，如有侵权请联系删除。*
